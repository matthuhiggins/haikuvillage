- content_for(:title) do
  = @title || 'Exploring haikus'

- content_for(:submenu) do
  %span#sub-menu
    = link_to "Recent", :controller => 'haikus', :action => 'index'
    = '/'
    = link_to "Popular", :controller => 'haikus', :action => 'popular'
    - if current_user
    = '/'
    = link_to "Favorites", :controller => 'favorites', :action => 'index'
    - end

= render(:partial => "shared/haikus", :object => @haikus)