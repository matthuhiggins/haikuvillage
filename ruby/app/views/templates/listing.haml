%h2= @title

%span= link_to 'Recent', :order => 'recent'
%span= link_to 'Most Viewed', :order => 'most_viewed'
%span= link_to 'Top Favorites', :order => 'top_favorites'

= render :partial => 'shared/haiku', :collection => @haikus
%br
= will_paginate @haikus, :id => 'pagination', :prev_label => 'Previous', :next_label => 'Next', :page_links => @page_links