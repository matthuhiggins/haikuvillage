- if inspiration
  .inspiration
    = link_to :id => inspiration do
      = image_tag inspiration.thumbnail
      - if inspiration.conversation_id.nil?
        Write a haiku
      - else
        = "#{inspiration.conversation.haikus_count_total} haikus"