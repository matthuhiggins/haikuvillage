<% title 'Your subjects' %>
<h2>Your subjects</h2>

<ul class="subject-list">
  <% @subjects.each do |subject| %>
  <li>
    <%= link_to subject.name, :id => subject.name %>
    <%= " (#{subject.count})" %>
  </li>
  <% end %>
</ul>