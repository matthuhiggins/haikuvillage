%span#sub-menu  
  = @sub_menu.map do |menu_item|
  - options = menu_item.last.is_a?(String) ? {:action => menu_item.last} : menu_item.last
  - link_to(menu_item.first.to_s, options)
  - end.join(' / ')