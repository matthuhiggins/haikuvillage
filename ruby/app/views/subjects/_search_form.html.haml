- form_tag subjects_path, :method => :get, :id => 'search_form' do
  = text_field_with_auto_complete :subject, :name, {:name => 'q', :value => params[:q], :size => 10}, {:url => suggest_subjects_path, :method => :get, :param_name => 'q'}
  = submit_tag 'Search', :name => nil